---
name: Settings
menu: Components
route: /components/settings
---

import { Playground, Props } from "docz";
import { Settings } from ".";

# Settings

`Settings` is a collapsable `<Card />` component that is used to handle all of
the settings for the plugin. This includes things like card and navigation
order.

## Usage

```ts
import { Settings } from "../components/Settings";
```

<Playground>
  {() => {
    const state = {
      navOrder: "",
      cardOrder: "",
      showCoreBlocks: "",
    };
    return <Settings state={state} onChange={handleChange} />;
    function handleChange(val) {
      console.log(val);
    }
  }}
</Playground>

## Props

<Props of={Settings} />
