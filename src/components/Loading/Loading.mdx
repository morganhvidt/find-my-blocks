---
name: Loading
menu: Components
route: /components/loading
---

import { Playground, Props } from "docz";
import { Logo } from "../Logo";
import { Loading } from ".";

# Loading

`Loading` is a component that shows a loading state. `Loading` is important to
use whenever the user is waiting for something to happen.

## Usage

```ts
import { Loading } from "../components/Loading";
```

<Playground>
  <Loading />
</Playground>
