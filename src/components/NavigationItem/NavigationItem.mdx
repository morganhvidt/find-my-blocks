---
name: NavigationItem
menu: Components
route: /components/navigation-item
---

import { Playground, Props } from "docz";
import { useState } from "react";
import { NavigationItem } from ".";

# NavigationItem

`NavigationItem` is a layout component that is used to show information about a
block. It is heavily used in the Sidebar component.

## Usage

```ts
import { NavigationItem } from "../components/NavigationItem";
```

<Playground>
  {() => {
    const [active, setActive] = useState(true);
    return (
      <>
        <NavigationItem
          name="Toronto Raptors"
          count={1}
          active={active}
          onClick={() => setActive(!active)}
        />
        <NavigationItem
          name="Los Angeles Lakers"
          count={1}
          onClick={() => setActive(!active)}
        />
        <NavigationItem
          name="Miami Heat"
          count={2}
          onClick={() => setActive(!active)}
        />
        <NavigationItem
          name="Boston Celtics"
          count={3}
          onClick={() => setActive(!active)}
        />
        <NavigationItem
          name="Brooklyn Nets"
          count={4}
          onClick={() => setActive(!active)}
        />
      </>
    );
  }}
</Playground>

## Props

<Props of={NavigationItem} />
